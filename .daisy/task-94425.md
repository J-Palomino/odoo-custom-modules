## Plan of Record for Odoo Task #94425

### Branch
- **Name**: `daisy/task-94425-exclude-expired-deals`
- **Base**: `main`

### Phases

#### Phase 1: Modify Data Models
- **Intended Change**: Update `mint.ptl.day` to filter out expired deals in the `action_publish` method. Modify `mint.ptl.deal` to ensure the `state` field accurately reflects expiration.
- **Files**:
  - `mint_command_center/models/ptl_day.py`
  - `mint_command_center/models/ptl_deal.py`
- **Verification**:
  - Run `py_compile` on modified files to ensure syntax correctness.

#### Phase 2: Testing and Validation
- **Intended Change**: Deploy changes to a staging environment and validate functionality.
- **Verification**:
  - Ensure expired deals are excluded from the publish process.
  - Verify the storefront homepage renders correctly without expired deals.

#### Phase 3: Production Deployment
- **Intended Change**: Deploy changes to production with `--update mint_command_center`.
- **Verification**:
  - Monitor the system for any issues related to deal publishing.

### Files To Touch
- `mint_command_center/models/ptl_day.py`
- `mint_command_center/models/ptl_deal.py`

### Verification Plan
- **Phase 1**: Run `py_compile` on modified files.
- **Phase 2**: Validate in staging environment.
- **Phase 3**: Monitor production deployment.

### Deviations from the Plan
- None identified at this stage.

### Execution Owner
- Note: Code execution is out-of-band by GitHub fleet or human developer.
